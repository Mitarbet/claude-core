# Memory files (mirrored from Windows)

Copy the claude-core Claude memory files here so Linux sessions can read them.
Source: `C:\Users\paulh\.claude\projects\C--DevEnv-claude-core\memory\`
(`MEMORY.md`, `project_claude_core_baseline.md`, and any others).

These are copies. The Windows memory directory stays the live copy until the
real `BASELINE.md` is written; re-copy after changes. Do not put secrets here.
